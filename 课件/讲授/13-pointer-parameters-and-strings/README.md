# CW-L13 指针参数与字符串复制：交换数值与复制字符

- 状态：`ready`
- 教材章节：第8章
- 课时：2课时，共90分钟
- 先修课次：`CW-L12`
- 核心程序：`QB-TR-028`、`QB-PG-017`
- 关联例程：`EX-C08-003`、`EX-C08-004`、`EX-C08-028`
- 关联题目：`QB-TR-028`、`QB-PG-017`、`QB-SC-064`、`QB-TF-007`、`QB-SC-045`、`QB-FB-008`、`QB-FB-004`
- 正式HTML：[index.html](index.html)

## 两个教学段

1. 通过指针形参交换调用者中的两个整数，区分交换所指对象与交换形参地址副本。
2. 使用字符指针形参同步遍历源数组和目标数组，逐字符复制并显式补写 `\0`。

HTML共36页，包含交换和字符串复制两个执行器、7道显式题库练习、8组状态选择、两组错误诊断和两组七项课后自检。两个学生版程序由校验器写入操作系统临时目录，使用MinGW GCC严格编译并执行5组行为测试后自动清理。

## 内容边界

- 字符串输入契约为长度不超过79且不含空白字符，使用 `scanf("%79s", source)`。
- 不使用 `fgets()`、字符串库函数、二级指针、结构体、文件、动态内存或函数指针。
- 地址仍按值传递；本课重点是通过解引用修改调用者对象。
- `QB-PG-033`与本课字符串复制主题重复，继续保留在题库但不作为核心程序。
- 教材例程只作为稳定引用，权威源码保持不变。

## 验证

```powershell
conda run -n base python tools/validate_courseware.py --id CW-L13
```

验证器检查36页结构、离线依赖、稳定链接、题库原文、正例边界、交互数量、UTF-8无BOM，以及两个学生版程序的5组编译与行为结果。
